# Pull data from API using requests package

In this repo we will try to use Python's 'requests' package to pull stock data from Quandl API. We can download 'requests' package using pip install requests.
